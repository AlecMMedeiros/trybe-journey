/*
In this first exercise, go through the array
and print out all the values in it with the console.log() function.
*/
let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];

for(let index = 0; index < numbers.length; index += 1) {
    console.log(numbers[index]);
}